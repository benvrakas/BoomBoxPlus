[AccessTransformers]
; Server reads each Boom Box's authoritative volume to replicate it to every client.
Accessor=(Class="AFGBoomBoxPlayer", Property="mState")
; Lets the mod push Custom Music position, song and play state to the open vanilla Boom Box page.
Accessor=(Class="AFGBoomBoxPlayer", Property="mStateListeners")
